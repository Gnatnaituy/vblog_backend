spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: vblog-user-server
        instance-id: vblog-user-server
      config:
        enabled: true
        format: yaml
        prefix: config
        data-key: configuration
        default-context: vblog-user-server