spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: vblog-post-server
        instance-id: vblog-post-server
      config:
        enabled: true
        format: yaml
        prefix: config
        data-key: configuration
        default-context: vblog-post-server