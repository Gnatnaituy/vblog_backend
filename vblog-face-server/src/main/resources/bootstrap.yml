spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: vblog-face-server
        instance-id: face-server-9005
      config:
        enabled: true
        format: yaml
        prefix: config
        data-key: configuration
        default-context: vblog-face-server
