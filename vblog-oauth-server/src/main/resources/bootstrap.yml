spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: vblog-oauth-server
        instance-id: vblog-oauth-server
      config:
        enabled: true
        format: properties
        prefix: config
        data-key: configuration
        default-context: vblog-oauth-server