spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: vblog-oauth-server
        instance-id: oauth-server-9003
      config:
        enabled: true
        format: yaml
        prefix: config
        data-key: configuration
        default-context: vblog-oauth-server
